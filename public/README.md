# Public 目录说明

## 背景图片

将背景图片命名为 `background.jpg` 并放置在此目录下。

图片要求：
- 宽度：建议至少 1920px（会自适应页面宽度）
- 高度：建议至少 350px
- 格式：JPG、PNG 或 WebP
- 文件大小：建议小于 2MB

如果未提供背景图片，系统会使用渐变背景作为后备。

## 上传文件

用户上传的文件会保存在 `uploads/` 子目录中。



